[project]
name = "gnmi-subscribe"
version = "0.1.0"
description = "gNMIc Subscribe Script - runs gnmic subscribe command and saves output"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
gnmi-subscribe = "gnmi_subscribe:main"
